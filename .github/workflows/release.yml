name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    strategy:
      matrix:
        go_version: [1.17.x]
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash    
    steps:
      #- name: Checkout
      #  uses: actions/checkout@v2
      - name: Build with xgo
        run: |
          pwd
          ls -al
          echo '${{ github.workspace }}'
          ls ${{ github.workspace }}
          go version
          printenv | sort
          mkdir build && cd build
          docker pull karalabe/xgo-latest
          echo '------------------------'
          go get github.com/karalabe/xgo
          ls -al ~/go/bin/
          ~/go/bin/xgo -v -x -ldflags='-s -w' github.com/stochmal/checkpath-go
          cd .. && ls -al build
      - name: Upload using GitHub cli
        run: |
          gh --version
          version="$(basename $GITHUB_REF)"
          echo $version
          gh release upload $version build/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

